{
  "name": "@jdion/web-configs",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@latest",
  "author": "Jonathan Dion <jonathandionalary@gmail.com> (https://github.com/jonathandion/)",
  "license": "MIT",
  "scripts": {
    "audit": "pnpm audit --audit-level high",
    "build": "turbo run build --color",
    "dev": "turbo run dev --color",
    "change": "changeset",
    "ci": "turbo run build types:check lint audit --color",
    "ci:debug": "act --secret-file my.secrets",
    "ci:publish": "pnpm publish -r",
    "ci:version": "changeset version && pnpm install --lockfile-only",
    "lint": "eslint ./ --ext js,jsx,ts,tsx,json,mdx,md --max-warnings=0",
    "lint:debug": "eslint . --debug",
    "lint:fix": "eslint ./ --ext js,jsx,ts,tsx,json,mdx,md --max-warnings=0 --fix"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "@jdion/eslint-config": "workspace:0.7.1",
    "concurrently": "^8.2.1",
    "eslint": "^8.48.0",
    "typescript": "^5.2.2"
  }
}
